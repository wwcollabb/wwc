<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>WinKom - Dashboard</title>
    <link rel="icon" type="image/png" href="/favicon.png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body { background: #0a0a0f; font-family: 'Plus Jakarta Sans', 'Segoe UI', sans-serif; -webkit-tap-highlight-color: transparent; }
        ::selection { background: rgba(34,211,238,0.3); }
        ::-webkit-scrollbar { width: 8px; height: 8px; }
        ::-webkit-scrollbar-track { background: transparent; }
        ::-webkit-scrollbar-thumb { background: rgba(34,211,238,0.25); border-radius: 8px; }
        .mesh-bg {
            background-color: #0a0a0f;
            background-image:
                radial-gradient(ellipse 600px 400px at 15% -5%, rgba(34,211,238,0.08), transparent 60%),
                radial-gradient(ellipse 500px 400px at 100% 10%, rgba(168,85,247,0.06), transparent 55%);
        }
        .glow-card { box-shadow: 0 0 30px rgba(34, 211, 238, 0.1); }
        .glow-btn { box-shadow: 0 0 20px rgba(34, 211, 238, 0.3); transition: all 0.3s; }
        .glow-btn:hover { box-shadow: 0 0 40px rgba(34, 211, 238, 0.6); transform: translateY(-2px); }
        .glow-btn:active { transform: scale(0.97); }
        .sidebar { transition: transform 0.3s ease; }
        .sidebar-open { transform: translateX(0); }
        .sidebar-closed { transform: translateX(-100%); }
        .fade-in { animation: fadeIn 0.5s ease; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(6px); } to { opacity: 1; transform: translateY(0); } }
        @media (min-width: 768px) { .sidebar-closed { transform: translateX(0); } }
        .progress-bar { transition: width 0.5s ease; }
        .investment-card { cursor: pointer; transition: all 0.3s; }
        .investment-card:hover { border-color: rgba(34, 211, 238, 0.5); }
        .investment-details { transition: max-height 0.3s ease; max-height: 0; overflow: hidden; }
        .investment-details.open { max-height: 200px; }
        .icon-badge { width: 44px; height: 44px; border-radius: 12px; display: flex; align-items: center; justify-content: center; font-size: 20px; }
        .stat-card { position: relative; overflow: hidden; }
        .withdraw-card { cursor: pointer; }
        .withdraw-card:hover .withdraw-arrow { transform: translateX(4px); }
        .withdraw-card:active { transform: scale(0.98); }
        .plan-card { min-height: 300px; background-color: #12121a; }
        .plan-card:hover { transform: translateY(-4px); }
        .modal-panel { animation: modalIn 0.25s cubic-bezier(0.16, 1, 0.3, 1); }
        @keyframes modalIn { from { opacity: 0; transform: translateY(12px) scale(0.98); } to { opacity: 1; transform: translateY(0) scale(1); } }
        .glass-header { background: rgba(18,18,26,0.75); backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px); }
        .nav-link { position: relative; transition: all 0.2s; }
        .nav-link.active::before { content: ''; position: absolute; left: 0; top: 50%; transform: translateY(-50%); width: 3px; height: 60%; background: linear-gradient(180deg, #22d3ee, #3b82f6); border-radius: 0 4px 4px 0; }
    </style>
</head>
<body class="min-h-screen mesh-bg">
    <!-- Sidebar -->
    <div id="sidebar" class="sidebar sidebar-closed fixed top-0 left-0 h-full w-64 bg-[#0d0d14] border-r border-white/5 z-50 overflow-y-auto">
        <div class="p-5">
            <div class="flex items-center justify-between mb-8">
                <div class="flex items-center gap-3">
                    <img src="logo.png" alt="Logo" class="w-10 h-10 rounded-2xl" onerror="this.style.display='none'"/>
                    <span class="text-xl font-extrabold text-white tracking-tight">Win<span class="text-cyan-400">Kom</span></span>
                </div>
                <button onclick="toggleSidebar()" class="md:hidden text-gray-400 hover:text-white">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
                </button>
            </div>
            <nav class="space-y-1.5">
                <a href="index.html" class="nav-link active flex items-center gap-3 px-4 py-3 rounded-2xl bg-cyan-500/15 text-cyan-300 font-semibold text-sm">
                    <span class="text-base">📊</span> Dashboard</a>
                <a href="deposit.html" class="nav-link flex items-center gap-3 px-4 py-3 rounded-2xl text-gray-400 hover:bg-white/5 hover:text-white transition text-sm">
                    <span class="text-base">💰</span> Deposit</a>
                <a href="withdraw.html" class="nav-link flex items-center gap-3 px-4 py-3 rounded-2xl text-gray-400 hover:bg-white/5 hover:text-white transition text-sm">
                    <span class="text-base">🏦</span> Withdraw</a>
                <a href="transactions.html" class="nav-link flex items-center gap-3 px-4 py-3 rounded-2xl text-gray-400 hover:bg-white/5 hover:text-white transition text-sm">
                    <span class="text-base">📋</span> My Transactions</a>
                <a href="settings.html" class="nav-link flex items-center gap-3 px-4 py-3 rounded-2xl text-gray-400 hover:bg-white/5 hover:text-white transition text-sm">
                    <span class="text-base">⚙️</span> Settings</a>
                <a href="about.html" class="nav-link flex items-center gap-3 px-4 py-3 rounded-2xl text-gray-400 hover:bg-white/5 hover:text-white transition text-sm">
                    <span class="text-base">ℹ️</span> About</a>
                <a href="support.html" class="nav-link flex items-center gap-3 px-4 py-3 rounded-2xl text-gray-400 hover:bg-white/5 hover:text-white transition text-sm">
                    <span class="text-base">🎧</span> Support</a>
                <div class="pt-3 mt-3 border-t border-white/5">
                    <button onclick="handleLogout()" class="w-full flex items-center gap-3 text-left px-4 py-3 rounded-2xl text-red-400 hover:bg-red-500/10 transition text-sm font-medium">
                        <span class="text-base">🚪</span> Logout</button>
                </div>
            </nav>
        </div>
    </div>

    <div id="overlay" onclick="toggleSidebar()" class="fixed inset-0 bg-black/50 z-40 hidden md:hidden"></div>

    <div class="md:ml-64">
        <header class="glass-header border-b border-white/5 p-4 sticky top-0 z-30">
            <div class="flex items-center justify-between max-w-6xl mx-auto">
                <div class="flex items-center gap-3">
                    <button onclick="toggleSidebar()" class="md:hidden text-gray-300 hover:text-white">
                        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/></svg>
                    </button>
                    <div>
                        <h2 class="text-lg font-bold text-white">Welcome, <span id="userName" class="text-cyan-400">Loading...</span></h2>
                        <p class="text-xs text-gray-500">Your Investment Dashboard</p>
                    </div>
                </div>
                <div class="flex items-center gap-3">
                    <div class="relative hidden sm:block">
                        <span class="absolute left-3 top-1/2 -translate-y-1/2 text-gray-500 text-sm pointer-events-none">🔍</span>
                        <input id="planSearchInput" type="text" inputmode="search" placeholder="Search plan or amount..."
                            oninput="filterPlans(this.value)"
                            class="bg-[#1a1a28] border border-gray-700/50 focus:border-cyan-400/60 outline-none text-white text-sm rounded-full pl-9 pr-4 py-2 w-48 md:w-64 transition-all placeholder-gray-500"/>
                    </div>
                    <span class="hidden sm:inline-flex items-center gap-1.5 text-xs bg-green-500/10 text-green-400 border border-green-500/30 px-3 py-1.5 rounded-full shrink-0">
                        <span class="w-1.5 h-1.5 rounded-full bg-green-400 animate-pulse"></span> Live
                    </span>
                </div>
            </div>
            <div class="max-w-6xl mx-auto mt-3 sm:hidden">
                <div class="relative">
                    <span class="absolute left-3 top-1/2 -translate-y-1/2 text-gray-500 text-sm pointer-events-none">🔍</span>
                    <input id="planSearchInputMobile" type="text" inputmode="search" placeholder="Search plan or amount..."
                        oninput="filterPlans(this.value)"
                        class="w-full bg-[#1a1a28] border border-gray-700/50 focus:border-cyan-400/60 outline-none text-white text-sm rounded-full pl-9 pr-4 py-2 transition-all placeholder-gray-500"/>
                </div>
            </div>
        </header>

        <main class="p-4 md:p-6 fade-in max-w-6xl mx-auto">
            <div id="dashboardSections">
            <!-- Stats Cards -->
            <!-- Wallet Card -->
            <div onclick="window.location.href='withdraw.html'" class="withdraw-card bg-gradient-to-br from-cyan-500/15 via-[#12121a] to-[#12121a] rounded-2xl p-6 border border-cyan-400/40 hover:border-cyan-300/70 hover:shadow-[0_0_35px_rgba(34,211,238,0.35)] transition-all duration-300 flex items-center justify-between gap-4 mb-4">
                <div>
                    <div class="flex items-center gap-2 mb-2">
                        <span class="icon-badge bg-cyan-500/15">💎</span>
                        <span class="text-gray-300 text-sm font-medium">Available Balance</span>
                    </div>
                    <p class="text-3xl md:text-4xl font-bold text-white">₹<span id="totalBalance">0</span></p>
                </div>
                <div class="flex items-center gap-1.5 text-cyan-300 text-sm font-semibold bg-cyan-500/10 border border-cyan-400/30 px-4 py-2.5 rounded-lg whitespace-nowrap">
                    <span>🏦 Withdraw</span>
                    <svg class="withdraw-arrow w-4 h-4 transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/></svg>
                </div>
            </div>

            <!-- Combined Stats Row -->
            <div class="grid grid-cols-3 bg-[#12121a] rounded-2xl glow-card border border-gray-700/40 divide-x divide-gray-700/40 overflow-hidden mb-8">
                <div onclick="openActiveInvestmentsModal()" class="cursor-pointer p-4 sm:p-5 text-center hover:bg-white/5 transition">
                    <div class="flex justify-center mb-2"><span class="icon-badge bg-purple-500/15">📈</span></div>
                    <p class="text-lg sm:text-xl font-bold text-white"><span id="activeInvestments">0</span></p>
                    <p class="text-[11px] sm:text-xs text-gray-500 mt-1">Active Investments</p>
                </div>
                <div class="p-4 sm:p-5 text-center">
                    <div class="flex justify-center mb-2"><span class="icon-badge bg-yellow-500/15">🤝</span></div>
                    <p class="text-lg sm:text-xl font-bold text-white">₹<span id="referralEarnings">0</span></p>
                    <p class="text-[11px] sm:text-xs text-gray-500 mt-1">Referral Earnings</p>
                </div>
                <div class="p-4 sm:p-5 text-center">
                    <div class="flex justify-center mb-2"><span class="icon-badge bg-green-500/15">👥</span></div>
                    <p class="text-lg sm:text-xl font-bold text-white"><span id="referralCount">0</span></p>
                    <p class="text-[11px] sm:text-xs text-gray-500 mt-1">People Referred</p>
                </div>
            </div>

            <!-- Referral Section -->
            <div class="bg-gradient-to-br from-[#12121a] to-[#161622] rounded-2xl p-6 glow-card border border-cyan-500/20 mb-8">
                <h3 class="text-xl font-bold text-white mb-2">Earn <span class="text-cyan-400">₹79</span> per referral — your friend gets <span class="text-yellow-400">₹59</span> too</h3>
                <p class="text-gray-400 mb-5 text-sm">Share your link below and get rewarded every time a friend joins.</p>

                <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 mb-5">
                    <div class="bg-black/25 rounded-lg p-3 border border-gray-700/50">
                        <p class="text-gray-500 text-xs mb-1">Your Referral Code</p>
                        <p class="text-cyan-400 font-bold text-lg tracking-wide" id="myReferralCode">Loading...</p>
                    </div>
                    <div class="bg-black/25 rounded-lg p-3 border border-gray-700/50">
                        <p class="text-gray-500 text-xs mb-1">Your Referral Link</p>
                        <p class="text-gray-300 text-xs truncate" id="myReferralLink">Loading...</p>
                    </div>
                </div>

                <div class="flex flex-wrap gap-3">
                    <button onclick="shareWhatsApp()" 
                        class="glow-btn bg-green-600 hover:bg-green-700 text-white font-bold py-2.5 px-4 rounded-lg flex items-center gap-2 text-sm">
                        📱 Share on WhatsApp
                    </button>
                    <button onclick="copyReferralLink()"
                        class="glow-btn bg-cyan-600 hover:bg-cyan-700 text-white font-bold py-2.5 px-4 rounded-lg flex items-center gap-2 text-sm">
                        🔗 Copy Referral Link
                    </button>
                </div>
            </div>
            </div>

            <!-- Available Plans -->
            <div class="mb-8">
                <div class="flex items-center gap-3 mb-4">
                    <span class="icon-badge bg-cyan-500/15">📊</span>
                    <h3 class="text-xl font-bold text-white">Available Investment Plans</h3>
                </div>
                <div id="plansContainer" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                    <p class="text-gray-400 col-span-full text-center py-8">Loading plans...</p>
                </div>
            </div>

            <!-- Active Investments Modal -->
            <div id="activeInvestmentsModal" class="hidden fixed inset-0 bg-black/70 backdrop-blur-sm z-50 flex items-center justify-center p-4">
                <div class="modal-panel bg-[#12121a] rounded-2xl p-6 w-full max-w-lg border border-purple-500/30 max-h-[85vh] overflow-y-auto">
                    <div class="flex items-center justify-between mb-4">
                        <h4 class="text-lg font-bold text-white flex items-center gap-2"><span class="icon-badge bg-purple-500/15">📈</span> Your Active Investments</h4>
                        <button onclick="closeActiveInvestmentsModal()" class="text-gray-400 hover:text-white text-xl leading-none">✕</button>
                    </div>
                    <div id="activeInvestmentsContainer" class="space-y-4">
                        <p class="text-gray-400 text-center py-8">Loading your investments...</p>
                    </div>
                </div>
            </div>

            <!-- Invest Modal -->
            <div id="investModal" class="hidden fixed inset-0 bg-black/70 backdrop-blur-sm z-50 flex items-center justify-center p-4">
                <div class="modal-panel bg-[#12121a] rounded-2xl p-6 w-full max-w-md border border-cyan-500/30">
                    <h4 class="text-lg font-bold text-white mb-2">Invest in <span id="investPlanName" class="text-cyan-400"></span></h4>
                    <p class="text-gray-400 text-sm mb-4">
                        Daily Return: <span id="investPlanReturn" class="text-yellow-400 font-bold"></span>% | 
                        Duration: <span id="investPlanDuration" class="text-yellow-400 font-bold"></span> days
                    </p>
                    <p class="text-gray-400 text-sm mb-4">
                        Amount Range: ₹<span id="investPlanMin" class="text-white"></span> - ₹<span id="investPlanMax" class="text-white"></span>
                    </p>
                    <div class="mb-4">
                        <label class="text-gray-300 text-sm mb-1 block">Investment Amount (₹)</label>
                        <input type="number" id="investAmount" min="1" placeholder="Enter amount" oninput="updateMaturityPreview()"
                            class="w-full bg-[#1a1a28] border border-gray-700 rounded-lg px-4 py-3 text-white placeholder-gray-500 focus:outline-none focus:border-cyan-400 transition"/>
                        <p id="investError" class="hidden text-red-400 text-xs mt-1"></p>
                    </div>
                    <div id="maturityPreview" class="hidden bg-cyan-500/10 border border-cyan-500/30 rounded-lg p-3 mb-4">
                        <div class="flex justify-between items-center text-sm">
                            <span class="text-gray-400">Maturity amount after <span id="maturityDuration"></span> days</span>
                            <span class="text-cyan-400 font-bold">₹<span id="maturityAmount">0</span></span>
                        </div>
                    </div>
                    <div class="flex gap-3">
                        <button onclick="confirmInvest()" class="glow-btn flex-1 bg-gradient-to-r from-cyan-500 to-blue-600 text-white font-bold py-3 rounded-lg">Confirm Investment</button>
                        <button onclick="closeInvestModal()" class="bg-gray-700 text-white font-bold py-3 px-4 rounded-lg hover:bg-gray-600 transition">Cancel</button>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <script type="module">
        import { initializeApp } from "https://www.gstatic.com/firebasejs/10.8.0/firebase-app.js";
        import { getAuth, signOut, onAuthStateChanged } from "https://www.gstatic.com/firebasejs/10.8.0/firebase-auth.js";
        import { getFirestore, doc, getDoc, updateDoc, collection, addDoc, query, where, getDocs, serverTimestamp } from "https://www.gstatic.com/firebasejs/10.8.0/firebase-firestore.js";

        const firebaseConfig = {
            apiKey: "AIzaSyCFfM5dHaVdt8q0bhGnswFYfeeYzyJ61GY",
            authDomain: "winkom-da4c2.firebaseapp.com",
            projectId: "winkom-da4c2",
            storageBucket: "winkom-da4c2.firebasestorage.app",
            messagingSenderId: "629229462242",
            appId: "1:629229462242:web:5ca49b09a8a5aadf92f4bb"
        };

        const app = initializeApp(firebaseConfig);
        const auth = getAuth(app);
        const db = getFirestore(app);

        let currentUser = null;
        let currentUserData = null;
        let selectedPlan = null;
        let plansList = [];

        window.toggleSidebar = function() {
            const sidebar = document.getElementById('sidebar');
            const overlay = document.getElementById('overlay');
            if (sidebar.classList.contains('sidebar-closed')) {
                sidebar.classList.remove('sidebar-closed');
                sidebar.classList.add('sidebar-open');
                overlay.classList.remove('hidden');
            } else {
                sidebar.classList.add('sidebar-closed');
                sidebar.classList.remove('sidebar-open');
                overlay.classList.add('hidden');
            }
        };

        window.handleLogout = async function() {
            try {
                await signOut(auth);
                window.location.href = 'login.html';
            } catch (err) {
                alert('Logout error: ' + err.message);
            }
        };

        window.shareWhatsApp = function() {
            if (!currentUserData || !currentUserData.referralCode) return;
            const link = `https://winkom.site/signup.html?ref=${currentUserData.referralCode}`;
            const msg = `🎉 Join WinKom today and get ₹59 bonus instantly! ✨\n💰 Sign up with my referral link and start earning:\n🔗 ${link}\n\n💎 WinKom - Your Trusted Investment Platform`;
            window.open(`https://wa.me/?text=${encodeURIComponent(msg)}`, '_blank');
        };

        window.copyReferralLink = function() {
            if (!currentUserData || !currentUserData.referralCode) return;
            const link = `https://winkom.site/signup.html?ref=${currentUserData.referralCode}`;
            navigator.clipboard.writeText(link).then(() => {
                alert('✅ Referral link copied! Share it with your friends.');
            }).catch(() => {
                prompt('Copy this link:', link);
            });
        };

        window.toggleInvestmentDetails = function(invId) {
            const details = document.getElementById(`details-${invId}`);
            if (details) {
                details.classList.toggle('open');
            }
        };

        window.openActiveInvestmentsModal = function() {
            document.getElementById('activeInvestmentsModal').classList.remove('hidden');
        };

        window.closeActiveInvestmentsModal = function() {
            document.getElementById('activeInvestmentsModal').classList.add('hidden');
        };

        function calculateMaturity(amount, dailyReturn, duration) {
            return amount + (amount * (dailyReturn / 100) * duration);
        }

        window.updateMaturityPreview = function() {
            const amount = parseFloat(document.getElementById('investAmount').value);
            const preview = document.getElementById('maturityPreview');
            if (!amount || isNaN(amount) || amount <= 0 || !selectedPlan) {
                preview.classList.add('hidden');
                return;
            }
            const maturity = calculateMaturity(amount, selectedPlan.dailyReturn, selectedPlan.duration);
            document.getElementById('maturityAmount').textContent = maturity.toFixed(2);
            document.getElementById('maturityDuration').textContent = selectedPlan.duration;
            preview.classList.remove('hidden');
        };

        window.openInvestModal = function(planId) {
            selectedPlan = plansList.find(p => p.id === planId);
            if (!selectedPlan) return;
            document.getElementById('investPlanName').textContent = selectedPlan.name;
            document.getElementById('investPlanReturn').textContent = selectedPlan.dailyReturn;
            document.getElementById('investPlanDuration').textContent = selectedPlan.duration;
            document.getElementById('investPlanMin').textContent = selectedPlan.minAmount;
            document.getElementById('investPlanMax').textContent = selectedPlan.maxAmount;
            document.getElementById('investAmount').value = '';
            document.getElementById('investError').classList.add('hidden');
            document.getElementById('maturityPreview').classList.add('hidden');
            document.getElementById('investModal').classList.remove('hidden');
        };

        window.closeInvestModal = function() {
            document.getElementById('investModal').classList.add('hidden');
            selectedPlan = null;
            document.getElementById('investError').classList.add('hidden');
            document.getElementById('maturityPreview').classList.add('hidden');
        };

        window.confirmInvest = async function() {
            const amount = parseFloat(document.getElementById('investAmount').value);
            const errorEl = document.getElementById('investError');

            if (!amount || isNaN(amount) || amount <= 0) {
                errorEl.textContent = '❌ Please enter a valid amount.';
                errorEl.classList.remove('hidden');
                return;
            }
            if (amount < selectedPlan.minAmount) {
                errorEl.textContent = `❌ Minimum investment for this plan is ₹${selectedPlan.minAmount}.`;
                errorEl.classList.remove('hidden');
                return;
            }
            if (amount > selectedPlan.maxAmount) {
                errorEl.textContent = `❌ Maximum investment for this plan is ₹${selectedPlan.maxAmount}.`;
                errorEl.classList.remove('hidden');
                return;
            }
            if (amount > (currentUserData?.balance || 0)) {
                errorEl.textContent = `❌ Insufficient balance. You have ₹${(currentUserData?.balance || 0).toFixed(2)}.`;
                errorEl.classList.remove('hidden');
                return;
            }
            errorEl.classList.add('hidden');

            try {
                const newBalance = currentUserData.balance - amount;
                await updateDoc(doc(db, 'users', currentUser.uid), { balance: newBalance });

                const now = new Date();
                const endDate = new Date(now);
                endDate.setDate(endDate.getDate() + parseInt(selectedPlan.duration));
                const maturityAmount = calculateMaturity(amount, selectedPlan.dailyReturn, selectedPlan.duration);

                await addDoc(collection(db, 'investments'), {
                    uid: currentUser.uid,
                    userName: currentUserData.name,
                    planId: selectedPlan.id,
                    planName: selectedPlan.name,
                    amount: amount,
                    dailyReturn: selectedPlan.dailyReturn,
                    duration: selectedPlan.duration,
                    maturityAmount: maturityAmount,
                    startDate: now,
                    endDate: endDate,
                    status: 'active',
                    createdAt: serverTimestamp()
                });

                await addDoc(collection(db, 'transactions'), {
                    uid: currentUser.uid,
                    type: 'investment',
                    description: `Investment in ${selectedPlan.name}`,
                    amount: -amount,
                    balanceAfter: newBalance,
                    createdAt: serverTimestamp()
                });

                alert('✅ Investment successful!');
                closeInvestModal();
                await loadUserData();
                await loadActiveInvestments();
                await loadPlans();
            } catch (err) {
                alert('Investment error: ' + err.message);
            }
        };

        async function loadUserData() {
            if (!currentUser) return;
            const userRef = doc(db, 'users', currentUser.uid);
            const userSnap = await getDoc(userRef);
            if (userSnap.exists()) {
                currentUserData = userSnap.data();
                document.getElementById('userName').textContent = currentUserData.name || 'User';
                document.getElementById('totalBalance').textContent = (currentUserData.balance || 0).toFixed(2);
                document.getElementById('referralEarnings').textContent = (currentUserData.totalReferralEarnings || 0).toFixed(2);
                document.getElementById('referralCount').textContent = currentUserData.referralCount || 0;
                document.getElementById('myReferralCode').textContent = currentUserData.referralCode || 'N/A';
                document.getElementById('myReferralLink').textContent = `https://winkom.site/signup.html?ref=${currentUserData.referralCode || ''}`;
            }
        }

        async function loadActiveInvestments() {
            if (!currentUser) return;
            const container = document.getElementById('activeInvestmentsContainer');
            const q = query(collection(db, 'investments'), where('uid', '==', currentUser.uid), where('status', '==', 'active'));
            const snap = await getDocs(q);

            let investments = [];
            snap.forEach(d => investments.push({ id: d.id, ...d.data() }));
            investments.sort((a, b) => (b.createdAt?.toDate?.() || new Date()) - (a.createdAt?.toDate?.() || new Date()));

            document.getElementById('activeInvestments').textContent = investments.length;

            if (investments.length === 0) {
                container.innerHTML = '<p class="text-gray-400 text-center py-8">No active investments yet. Start investing today!</p>';
                return;
            }

            container.innerHTML = investments.map(inv => {
                const startDate = inv.startDate?.toDate?.() || inv.createdAt?.toDate?.() || new Date();
                const endDate = inv.endDate?.toDate?.() || new Date(startDate.getTime() + inv.duration * 24 * 60 * 60 * 1000);
                const now = new Date();
                const totalDuration = endDate.getTime() - startDate.getTime();
                const elapsed = now.getTime() - startDate.getTime();
                const progress = Math.min(100, Math.max(0, (elapsed / totalDuration) * 100));
                const daysRemaining = Math.max(0, Math.ceil((endDate.getTime() - now.getTime()) / (1000 * 60 * 60 * 24)));
                const maturityAmount = inv.maturityAmount ?? calculateMaturity(inv.amount, inv.dailyReturn, inv.duration);

                return `
                <div class="investment-card bg-[#12121a] rounded-2xl p-5 glow-card border border-purple-500/20" onclick="toggleInvestmentDetails('${inv.id}')">
                    <div class="flex flex-wrap justify-between items-center mb-3">
                        <div>
                            <h4 class="text-lg font-bold text-white">${inv.planName}</h4>
                            <p class="text-gray-400 text-sm">Invested: ₹${inv.amount.toFixed(2)}</p>
                        </div>
                        <span class="px-3 py-1 rounded-full text-xs bg-green-500/20 text-green-400">● Active</span>
                    </div>
                    <div class="flex justify-between items-center mb-3 bg-gradient-to-r from-cyan-500/10 to-transparent rounded-lg px-3 py-2">
                        <span class="text-gray-400 text-xs">Maturity Amount</span>
                        <span class="text-cyan-400 font-bold">₹${maturityAmount.toFixed(2)}</span>
                    </div>
                    <div class="grid grid-cols-3 gap-2 text-center mb-3">
                        <div class="bg-gray-800/50 rounded-lg p-2">
                            <p class="text-gray-500 text-xs">Daily Return</p>
                            <p class="text-yellow-400 font-bold">${inv.dailyReturn}%</p>
                        </div>
                        <div class="bg-gray-800/50 rounded-lg p-2">
                            <p class="text-gray-500 text-xs">Days Remaining</p>
                            <p class="text-white font-bold">${daysRemaining}</p>
                        </div>
                        <div class="bg-gray-800/50 rounded-lg p-2">
                            <p class="text-gray-500 text-xs">End Date</p>
                            <p class="text-white font-bold text-xs">${endDate.toLocaleDateString()}</p>
                        </div>
                    </div>
                    <div class="mt-2">
                        <div class="flex justify-between text-xs text-gray-400 mb-1">
                            <span>Start: ${startDate.toLocaleDateString()}</span>
                            <span>${Math.round(progress)}% Complete</span>
                        </div>
                        <div class="w-full bg-gray-700 rounded-full h-2">
                            <div class="progress-bar bg-gradient-to-r from-cyan-500 to-blue-600 rounded-full h-2" style="width: ${progress}%"></div>
                        </div>
                    </div>
                    <div id="details-${inv.id}" class="investment-details mt-3">
                        <div class="bg-gray-800/30 rounded-lg p-3 text-sm">
                            <p class="text-gray-400">Total Duration: ${inv.duration} days</p>
                            <p class="text-gray-400">Invested Amount: ₹${inv.amount.toFixed(2)}</p>
                            <p class="text-gray-400">Daily Return: ${inv.dailyReturn}%</p>
                            <p class="text-gray-400">Maturity Amount: ₹${maturityAmount.toFixed(2)}</p>
                            <p class="text-gray-400">Start Date: ${startDate.toLocaleDateString()}</p>
                            <p class="text-gray-400">End Date: ${endDate.toLocaleDateString()}</p>
                            <p class="text-gray-400">Days Remaining: ${daysRemaining}</p>
                        </div>
                    </div>
                </div>`;
            }).join('');
        }

        const planImages = [
            'https://images.unsplash.com/photo-1611974789855-9c2a0a7236a3?auto=format&fit=crop&w=800&q=70',
            'https://images.unsplash.com/photo-1621761191319-c6fb62004040?auto=format&fit=crop&w=800&q=70',
            'https://images.unsplash.com/photo-1590283603385-17ffb3a7f29f?auto=format&fit=crop&w=800&q=70',
            'https://images.unsplash.com/photo-1638913662252-70efce1e60d1?auto=format&fit=crop&w=800&q=70',
            'https://images.unsplash.com/photo-1642104704074-907c0698cbd9?auto=format&fit=crop&w=800&q=70',
            'https://images.unsplash.com/photo-1559526324-593bc073d938?auto=format&fit=crop&w=800&q=70'
        ];

        window.filterPlans = function(query) {
            const q = (query || '').trim().toLowerCase();

            const desktopInput = document.getElementById('planSearchInput');
            const mobileInput = document.getElementById('planSearchInputMobile');
            if (desktopInput && desktopInput.value !== query) desktopInput.value = query;
            if (mobileInput && mobileInput.value !== query) mobileInput.value = query;

            const dashboardSections = document.getElementById('dashboardSections');

            if (q === '') {
                if (dashboardSections) dashboardSections.classList.remove('hidden');
                renderPlans(plansList);
                return;
            }

            if (dashboardSections) dashboardSections.classList.add('hidden');

            const amount = parseFloat(q);
            const filtered = plansList.filter(plan => {
                const nameMatch = (plan.name || '').toLowerCase().includes(q);
                const amountMatch = !isNaN(amount) &&
                    amount >= Number(plan.minAmount) && amount <= Number(plan.maxAmount);
                return nameMatch || amountMatch;
            });

            renderPlans(filtered);
        };

        function renderPlans(plans) {
            const container = document.getElementById('plansContainer');

            if (plans.length === 0) {
                container.innerHTML = '<p class="text-gray-400 col-span-full text-center py-8">No matching investment plans found.</p>';
                return;
            }

            container.innerHTML = plans.map((plan) => {
                const idx = plansList.indexOf(plan);
                const img = planImages[idx % planImages.length];
                return `
                <div class="plan-card rounded-2xl overflow-hidden border border-cyan-500/25 hover:border-cyan-400/60 hover:shadow-[0_0_30px_rgba(34,211,238,0.3)] transition-all duration-300 bg-[#12121a]">
                    <div class="relative h-36" style="background-image: linear-gradient(180deg, rgba(8,10,18,0.05) 0%, rgba(8,10,18,0.35) 65%, rgba(8,10,18,0.95) 100%), url('${img}'); background-size: cover; background-position: center;">
                        <span class="absolute top-3 right-3 text-[11px] bg-gradient-to-r from-yellow-400 to-amber-500 text-black px-2.5 py-1 rounded-full font-extrabold shadow-lg">⚡ ${plan.dailyReturn}%/day</span>
                        <h4 class="absolute bottom-3 left-4 text-white font-bold text-lg drop-shadow-[0_2px_4px_rgba(0,0,0,0.8)]">${plan.name}</h4>
                    </div>
                    <div class="p-5 flex flex-col">
                        <p class="text-gray-400 text-sm mb-4">${plan.description || 'Investment Plan'}</p>
                        <div class="grid grid-cols-2 gap-2 mb-4">
                            <div class="bg-[#1a1a28] rounded-lg p-2 border border-gray-700/50">
                                <p class="text-gray-500 text-[11px]">Min Amount</p>
                                <p class="text-white font-semibold text-sm">₹${plan.minAmount}</p>
                            </div>
                            <div class="bg-[#1a1a28] rounded-lg p-2 border border-gray-700/50">
                                <p class="text-gray-500 text-[11px]">Max Amount</p>
                                <p class="text-white font-semibold text-sm">₹${plan.maxAmount}</p>
                            </div>
                            <div class="bg-[#1a1a28] rounded-lg p-2 border border-gray-700/50">
                                <p class="text-gray-500 text-[11px]">Duration</p>
                                <p class="text-white font-semibold text-sm">${plan.duration} days</p>
                            </div>
                            <div class="bg-[#1a1a28] rounded-lg p-2 border border-gray-700/50">
                                <p class="text-gray-500 text-[11px]">Daily Return</p>
                                <p class="text-yellow-400 font-semibold text-sm">${plan.dailyReturn}%</p>
                            </div>
                        </div>
                        <button onclick="openInvestModal('${plan.id}')" class="glow-btn w-full bg-gradient-to-r from-cyan-500 to-blue-600 text-white font-bold py-2.5 px-4 rounded-lg text-sm">🚀 Invest Now</button>
                    </div>
                </div>`;
            }).join('');
        }

        async function loadPlans() {
            const container = document.getElementById('plansContainer');
            const snap = await getDocs(collection(db, 'plans'));
            plansList = [];
            snap.forEach(doc => {
                if (doc.data().status === 'active') {
                    plansList.push({ id: doc.id, ...doc.data() });
                }
            });

            if (plansList.length === 0) {
                container.innerHTML = '<p class="text-gray-400 col-span-full text-center py-8">No active investment plans available.</p>';
                return;
            }

            renderPlans(plansList);
        }

        onAuthStateChanged(auth, async (user) => {
            if (!user) {
                window.location.href = 'login.html';
                return;
            }
            currentUser = user;
            try { await loadUserData(); } catch (e) { console.error('loadUserData failed:', e); }
            try { await loadActiveInvestments(); } catch (e) { console.error('loadActiveInvestments failed:', e); }
            try { await loadPlans(); } catch (e) {
                console.error('loadPlans failed:', e);
                document.getElementById('plansContainer').innerHTML =
                    '<p class="text-red-400 col-span-full text-center py-8">⚠️ Could not load plans. Please refresh the page.</p>';
            }
        });
    </script>
</body>
</html>
